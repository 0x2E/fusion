# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = "{APP_NAME}"

# https://fly.io/docs/reference/regions/#fly-io-regions
primary_region = "{REGION}"

[build]
image = 'ghcr.io/patrickjmcd/reedme:latest'

[http_service]
internal_port = 8080
force_https = true
auto_stop_machines = "off"
auto_start_machines = false

[[vm]]
size = 'shared-cpu-1x'
memory = '256mb'

[env]
REEDME_PASSWORD = "{YOUR_PASSWORD}"
REEDME_HOST = "127.0.0.1"
REEDME_DB_PATH = "/data/reedme.db"

[mounts]
source = "reedme_data"
destination = "/data"
initial_size = "1GB"
